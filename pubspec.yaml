name: game_c
description: A new Flutter project.
publish_to: "none" # ป้องกันการ publish ไปยัง pub.dev โดยไม่ตั้งใจ

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.0
  # เพิ่มแพ็กเกจอื่น ๆ ที่คุณต้องการได้ตรงนี้

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
